# Code generated by craft; DO NOT EDIT.

version: 1

builds:
  - main: cmd/go-builder-generator/main.go
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/kilianpaquier/go-builder-generator/internal/cobra.version={{ .Version }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

announce:
  skip: true

changelog:
  skip: true

archives:
  - format: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"
  
nfpms:
  - maintainer: kilianpaquier
    license: MIT
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - apk
      - deb
      - rpm
