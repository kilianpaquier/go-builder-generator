# Code generated by kickr; DO NOT EDIT.

version: 2

checksum:
  name_template: checksums.txt
snapshot:
  version_template: "{{ .Env.VERSION }}"

builds:
  - main: ./cmd/go-builder-generator
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/kilianpaquier/go-builder-generator/internal/build.branch={{ .Branch }}
      - -X github.com/kilianpaquier/go-builder-generator/internal/build.commit={{ .Commit }}
      - -X github.com/kilianpaquier/go-builder-generator/internal/build.date={{ .Date }}
      - -X github.com/kilianpaquier/go-builder-generator/internal/build.version={{ .Env.VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip

nfpms:
  - maintainer: u.kilianpaquier
    license: MIT
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - deb
      - rpm
