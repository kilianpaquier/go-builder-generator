# Code generated by kickr; DO NOT EDIT.

default_install_hook_types:
  - commit-msg
  - pre-commit

default_stages:
  - pre-commit

repos:

  # see https://www.conventionalcommits.org/en/v1.0.0/#specification
  - repo: https://github.com/commitizen-tools/commitizen
    # renovate: datasource=github-tags packageName=commitizen-tools/commitizen depName=commitizen
    rev: v4.12.1
    hooks:
      - id: commitizen
      - id: commitizen-branch
        args:
          - --rev-range
          - ${GITHUB_BASE_REF:-${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:+origin/}}${GITHUB_BASE_REF:-${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:-HEAD~1}}..HEAD
        stages: [pre-commit]

  - repo: https://github.com/gitleaks/gitleaks
    # renovate: datasource=github-tags packageName=gitleaks/gitleaks depName=gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks

  - repo: https://github.com/pre-commit/pre-commit-hooks
    # renovate: datasource=github-tags packageName=pre-commit/pre-commit-hooks depName=pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: trailing-whitespace
